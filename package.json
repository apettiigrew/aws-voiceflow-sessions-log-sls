{
  "name": "aws-voiceflow-auto-end-chat-serverless",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/functions/submitChatEvent.ts src/functions/processChatEvent.ts --outdir=dist/functions --platform=node --target=node20 --format=cjs",
    "deploy": "npm run build && serverless deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-sqs": "^3.700.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "serverless": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
