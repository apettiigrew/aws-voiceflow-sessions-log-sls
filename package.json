{
  "name": "aws-voiceflow-auto-end-chat-serverless",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/functions/submit-chat-event.ts src/functions/process-chat-event.ts --outdir=dist/functions --platform=node --target=node20 --format=cjs",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-sqs": "^3.700.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "esbuild": "^0.24.0",
    "serverless": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
